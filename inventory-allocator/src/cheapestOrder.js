/**
 * Finds the cheapest way an object can be shipped given an object of orders and an array of warehouses
 * @param {Object} orders 
 * @param {Array} warehouses 
 * @returns {Array}
 */
const cheapestOrder = (
    orders = {}, //orders must be an arra
    warehouses = []
) => {
    
}